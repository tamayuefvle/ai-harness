# Independent review — IMP-20260814-05

## Scope

Read-only review of the v15 phase/gate redesign against the approved change: lifecycle/state contracts, migration safety, generated projections, execution/fallback binding, human approval boundaries, and regression evidence.

## Findings

| Severity | Finding | Resolution |
|---|---|---|
| HIGH | Project Design could initially reach final design approval after Planning documents changed without re-approving Planning. | Fixed. Final design gate requires a fresh Planning gate; `DESIGNING → PLANNING` is an explicit human rework transition that invalidates downstream project gates. |
| HIGH | Initial legacy task migration mapped old downstream states forward while resetting their evidence, which could create an impossible v15 state/evidence combination. | Fixed. Migration validates scope/design under a fresh human approval, backs up gate + active pointer, resets downstream evidence, and restarts old `IMPLEMENTING/VERIFYING/REVIEW_READY/DEPLOY_READY` tasks at `DEVELOPING`. |
| MEDIUM | Retired marker-owned Cursor Skills could survive canonical Skill deletion because generation only wrote current projections. | Fixed. Skill generation prunes only obsolete marker-owned generated Skills and preserves unmanaged user Skills; regression tests cover both cases. |
| MEDIUM | `record-gate.mjs` initially omitted the downstream-reset helper import during terminology migration. | Fixed and lifecycle-gate regression tests pass. |
| MEDIUM | Project migration could create a backup before detecting conflicting legacy/new technology documents, making a corrected retry awkward. | Fixed. Conflict preflight runs before backup creation. |
| MEDIUM | Final project design approval originally checked only profile-resolution status; a stale registry could be detected later at ACTIVE transition. | Fixed. Design approval now checks registry digest and proposed-profile coverage as well as resolved status. |

## Review result

- Unresolved BLOCKER: **0**
- Unresolved HIGH: **0**
- Unresolved MEDIUM: **0**
- LOW / INFO: aggregate `npm run verify:harness` was not completed as one process in this execution environment because the all-tests invocation timed out; the same constituent checks and all 46 test files were executed separately and passed. Schema validation used the documented standards-compliant Python `jsonschema` fallback because Ajv was unavailable in the execution environment.

**Verdict: ACCEPTED** for packaging as v15.0.0, subject to the recorded environment limitations above.
