import path from "node:path"; import { assertNoLikelySecret, canonicalRoot, parseOptions, safeId, writeJsonAtomic } from "./full-lifecycle-lib.mjs";
const o=parseOptions(process.argv.slice(2)); const id=safeId(o.id,/^SIG-\d{4}-\d{2}-\d{2}-[a-z0-9-]+$/,"signal id"); if(!["telemetry","user-feedback","incident","review","security","cost"].includes(o.type)) throw new Error("Invalid --type"); if(!["info","low","medium","high","critical"].includes(o.severity)) throw new Error("Invalid --severity"); if(!o.source||!o.summary) throw new Error("--source and --summary are required"); assertNoLikelySecret(`${o.source} ${o.summary} ${o.evidence??""}`); const record={schemaVersion:"1.0.0",signalId:id,type:o.type,source:o.source,observedAt:new Date().toISOString(),summary:o.summary,severity:o.severity,evidence:o.evidence?[o.evidence]:[],containsSecrets:false}; writeJsonAtomic(path.join(canonicalRoot,"docs/operations/signals",`${id}.json`),record); console.log(JSON.stringify(record,null,2));
