import path from "node:path"; import { canonicalRoot, parseOptions, readJson, safeId } from "./full-lifecycle-lib.mjs";
const o=parseOptions(process.argv.slice(2)); const id=safeId(o.id,/^REL-\d{4}-\d{2}-\d{2}-[a-z0-9-]+$/,"release id"); const expected=o.state??"PRODUCTION_APPROVED"; const gate=readJson(path.join(canonicalRoot,"docs/operations/releases",id,"gate.json")); if(gate.state!==expected) throw new Error(`Release ${id} is ${gate.state}; expected ${expected}.`); if(expected==="PRODUCTION_APPROVED" && (gate.productionApproval.status!=="approved" || !/^human:\S+$/.test(gate.productionApproval.approvedBy??""))) throw new Error("Production approval record is incomplete."); console.log(JSON.stringify({releaseId:id,state:gate.state,artifact:gate.artifact,productionApproval:gate.productionApproval},null,2));
