import path from "node:path"; import { assertNoLikelySecret, canonicalRoot, parseOptions, safeId, writeJsonAtomic } from "./full-lifecycle-lib.mjs";
const o=parseOptions(process.argv.slice(2)); const id=safeId(o.id,/^INC-\d{4}-\d{2}-\d{2}-[a-z0-9-]+$/,"incident id"); if(!/^(SEV1|SEV2|SEV3|SEV4)$/.test(o.severity??"")) throw new Error("--severity must be SEV1..SEV4"); if(!o.summary||!o.impact) throw new Error("--summary and --impact are required"); assertNoLikelySecret(`${o.summary} ${o.impact}`); const record={schemaVersion:"1.0.0",incidentId:id,state:"DETECTED",severity:o.severity,detectedAt:new Date().toISOString(),summary:o.summary,impact:o.impact,mitigationApproval:{status:"pending",approvedBy:null,approvedAt:null,reason:null},recovery:{status:"pending",evidence:null,recordedAt:null},postmortemPath:null,history:[]}; writeJsonAtomic(path.join(canonicalRoot,"docs/operations/incidents",id,"gate.json"),record); console.log(JSON.stringify({incidentId:id,state:record.state},null,2));
