import path from "node:path"; import { canonicalRoot, parseOptions, readJson, safeId, writeJsonAtomic } from "./full-lifecycle-lib.mjs";
const o=parseOptions(process.argv.slice(2)); const id=safeId(o.id,/^IMP-\d{4}-\d{2}-\d{2}-[a-z0-9-]+$/,"proposal id"); const signals=(o.signals??"").split(",").filter(Boolean); if(!signals.length||!o.summary||!o.action) throw new Error("--signals, --summary, and --action are required"); if(!["create-task","change-project-decision","monitor","reject"].includes(o.action)) throw new Error("Invalid --action"); for(const signal of signals) readJson(path.join(canonicalRoot,"docs/operations/signals",`${signal}.json`)); const record={schemaVersion:"1.0.0",proposalId:id,sourceSignals:signals,status:"proposed",summary:o.summary,recommendedAction:o.action,projectDecisionChange:o.action==="change-project-decision",approvedBy:null,approvedAt:null}; writeJsonAtomic(path.join(canonicalRoot,"docs/operations/improvements",`${id}.json`),record); console.log(JSON.stringify(record,null,2));
